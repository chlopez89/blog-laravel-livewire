# blog-laravel-livewire

## Instalación

Para instalar las dependencias del proyecto, sigue estos pasos:

1. Instala las dependencias de PHP usando Composer:
   ```
   composer install
   ```

2. Instala las dependencias de Node.js usando npm:
   ```
   npm install
   ```

Esto preparará el proyecto para su ejecución.

